import { SelfNotifications as render } from '@quillforms/notifications-editor';
export const subpanelSettings = {
	name: 'self-notifications',
	parent: 'notifications',
	title: 'Self Notifications',
	render,
};
