/**
 * Get the registered meta
 *
 * @param {Object} state       Global application state.
 *
 * @return {Object} Form structure
 */
export function getRegisteredMeta( state ) {
	return state.meta;
}
