import { BlockControls as render } from '@quillforms/block-editor';
export const panelSettings = {
	name: 'blockControls',
	title: 'Block Controls',
	isHidden: true,
	mode: 'single',
	areaToHide: '',
	render,
};
